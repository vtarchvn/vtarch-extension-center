name: Release VEC

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build RBZ package
        run: |
          rm -f VTARCH_VEC.rbz
          zip -r VTARCH_VEC.rbz vtarch_vec.rb vtarch_vec
          sha256sum VTARCH_VEC.rbz > VTARCH_VEC.rbz.sha256
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            VTARCH_VEC.rbz
            VTARCH_VEC.rbz.sha256
