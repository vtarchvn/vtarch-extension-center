# Plugin mẫu để kiểm thử

- `vec_sample.rb`: kiểm thử cài, backup, chuyển vào backup và khôi phục file `.rb`.
- `VEC_RBZ_Fixture.rbz`: kiểm thử cài, backup, chuyển và khôi phục một `.rbz` có thư mục con.

Hai plugin này chỉ thêm một mục menu nhỏ trong **Extensions** và không tác động model.
